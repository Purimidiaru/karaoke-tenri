import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App.js'; // Ajoutez l'extension .js ici
import reportWebVitals from './reportWebVitals.js'; // Ajoutez l'extension .js ici

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
